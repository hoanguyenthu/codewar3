import { Dimensions } from "react-native";

export default {
    Dimensions:{
        DIMENSION_WIDTH: Dimensions.get("window").width,
        DIMENSION_HEIGHT: Dimensions.get("window").height
    }
}